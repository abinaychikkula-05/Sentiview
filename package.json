{
  "name": "sentiview",
  "version": "1.0.0",
  "description": "SentiView: Full-stack Feedback Analysis Dashboard",
  "private": true,
  "scripts": {
    "build": "cd Frontend && npm install && npm run build",
    "vercel-build": "cd Frontend && npm install && npm run build"
  }
}
